export function load() {
  /** @type {import("$lib/seo/MetadataTypes").Metadata} */
  const metadata = {
    title: "Playground",
    description: "Play around with the ICU Message Format and the T18S Compiler.",
  };

  return { metadata };
}
